/**
 * PostgreSQL-Client für Cloud-Sync (optional).
 * Nach `npx prisma migrate dev` und Adapter-Setup aktivieren.
 * Siehe README – ohne DB läuft die App vollständig mit LocalStorage.
 */
export const prisma = null;
